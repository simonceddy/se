import React from 'react';

function FooterIconContainer({ children }) {
  return (
    <div className="flex items-center sm:text-5xl text-3xl">
      {children}
    </div>
  );
}

export default FooterIconContainer;
